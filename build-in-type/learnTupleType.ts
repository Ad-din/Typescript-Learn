//learn tuple type
let use:[number, string];
use=[11,"343"];//first index have to be number and second one string. cause we declared it in above;
console.log(use);
//to insert more than 1 record we can use push method.
use.push(33,"abdullah");
console.log(use);
